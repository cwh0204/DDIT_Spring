<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.mbti.mapper.MbtiMapper">
	<select id="selectOne"
		resultType="kr.or.ddit.mbti.dto.MbtiDTO">
		SELECT
		    MT_SORT,
		    MT_TYPE,
		    MT_TITLE,
		    MT_CONTENT
		FROM
		    TB_MBTIDESC
		WHERE
			MT_TYPE = #{mtType}
	</select>
	<select id="selectList"
		resultType="kr.or.ddit.mbti.dto.MbtiDTO">
		SELECT
		    MT_SORT,
		    MT_TYPE,
		    MT_TITLE,
		    MT_CONTENT
		FROM
		    TB_MBTIDESC
	</select>
	
	<insert id="insert">
		INSERT INTO TB_MBTIDESC 
			   VALUES (SEQ_MBTI.NEXTVAL,#{mtType},#{mtTitle},#{mtContent})
	</insert>
	
	<update id="update">
		UPDATE tb_mbtidesc
		SET
		    MT_TYPE = #{mtType},
		    MT_TITLE = #{mtTitle},
		    MT_CONTENT = #{mtContent}
		WHERE
		   mt_type = #{mtType}
	</update>
	
	<delete id="delete">
		DELETE FROM tb_mbtidesc
    	WHERE mt_type = #{mtType}
	</delete>
</mapper>